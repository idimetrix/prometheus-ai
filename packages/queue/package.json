{
  "name": "@prometheus/queue",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@prometheus/logger": "workspace:*",
    "bullmq": "^5.71.0",
    "ioredis": "5.10.1"
  },
  "devDependencies": {
    "@prometheus/config-typescript": "workspace:*",
    "@prometheus/types": "workspace:*",
    "typescript": "^6.0.2"
  }
}
