apiVersion: v1
kind: Service
metadata:
  name: project-brain
  namespace: prometheus
  labels:
    app: project-brain
spec:
  selector:
    app: project-brain
  ports:
    - port: 4003
      targetPort: 4003
      protocol: TCP
  type: ClusterIP
