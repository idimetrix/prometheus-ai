apiVersion: v1
kind: Service
metadata:
  name: sandbox-manager
  namespace: prometheus
  labels:
    app: sandbox-manager
spec:
  selector:
    app: sandbox-manager
  ports:
    - port: 4006
      targetPort: 4006
      protocol: TCP
  type: ClusterIP
