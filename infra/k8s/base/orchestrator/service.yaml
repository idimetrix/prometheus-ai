apiVersion: v1
kind: Service
metadata:
  name: orchestrator
  namespace: prometheus
  labels:
    app: orchestrator
spec:
  selector:
    app: orchestrator
  ports:
    - port: 4002
      targetPort: 4002
      protocol: TCP
  type: ClusterIP
