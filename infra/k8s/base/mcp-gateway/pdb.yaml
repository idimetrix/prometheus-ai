apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: mcp-gateway-pdb
  namespace: prometheus
  labels:
    app: mcp-gateway
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: mcp-gateway
